{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Start Vite",
            "type": "npm",
            "script": "dev",
            "problemMatcher": [],
            "isBackground": true,
            "options": {
                "cwd": "${workspaceFolder}"
            }
        }
    ]
}